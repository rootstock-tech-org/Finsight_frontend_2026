# Supabase Configuration
# Copy this content to .env.local and fill in your actual values

# Supabase Project URL (get this from your Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon Key (get this from your Supabase dashboard)
# This key will be automatically managed and refreshed
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (for admin operations - keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Database URL (if using external database)
DATABASE_URL=postgresql://username:password@host:port/database

# Supabase Edge Functions URL (optional)
NEXT_PUBLIC_SUPABASE_FUNCTIONS_URL=https://your-project-ref.supabase.co/functions/v1

# Environment
NODE_ENV=development

# Optional: Custom API endpoint for key fetching (if you set this up)
# NEXT_PUBLIC_SUPABASE_KEYS_API=/api/supabase-keys
